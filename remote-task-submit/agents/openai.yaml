interface:
  display_name: "Remote Task Submit"
  short_description: "Public template for customizing remote job submission"
  default_prompt: "Use $remote-task-submit as the cluster-specific submission adapter. Prepare the runnable workflow command first, then customize the scripts and placeholders in this skill to match the user's scheduler or remote server."
