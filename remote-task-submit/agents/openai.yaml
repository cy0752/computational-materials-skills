interface:
  display_name: "Remote Task Submit"
  short_description: "Legacy alias for the canonical Inspire submit workflow"
  default_prompt: "Use $inspire-cli-training-submit as the canonical generic Inspire submission flow. If the task matches a workflow-specific submit skill such as $inspire-openmx-submit or $inspire-hamgnn-submit, follow that skill's canonical defaults instead of inventing new ones."
